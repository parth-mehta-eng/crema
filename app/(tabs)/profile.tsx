import { StyleSheet, View } from 'react-native';import { Ionicons } from '@expo/vector-icons';import { Screen } from '@/components/Screen';import { DisplayText, AppText } from '@/components/AppText';import { colors, radius, spacing } from '@/constants/theme';
const rows=[['person-outline','Account'],['options-outline','Preferences'],['notifications-outline','Notifications'],['help-circle-outline','Help & feedback']];
export default function Profile(){return <Screen><View style={styles.head}><DisplayText style={styles.title}>Profile</DisplayText><AppText>Keep it simple. Sign-in comes in Weekend 3.</AppText></View><View style={styles.card}>{rows.map(([icon,label])=><View key={label} style={styles.row}><Ionicons name={icon as any} size={22} color={colors.espresso}/><AppText style={styles.label}>{label}</AppText><Ionicons name="chevron-forward" size={18} color={colors.textMuted}/></View>)}</View></Screen>}
const styles=StyleSheet.create({head:{padding:spacing.xl,paddingTop:spacing.xxl},title:{fontSize:40,lineHeight:44},card:{margin:spacing.xl,backgroundColor:colors.surface,borderRadius:radius.card,overflow:'hidden'},row:{height:62,paddingHorizontal:spacing.lg,flexDirection:'row',alignItems:'center',borderBottomWidth:1,borderBottomColor:colors.border},label:{flex:1,marginLeft:14}});
